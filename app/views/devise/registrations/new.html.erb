<div class="row">
  <div class="col-lg-4 col-md-6 ml-auto mr-auto">
    <h1 class="text-center">Регистрация</h1>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        <%= f.email_field :email, autofocus: false, class: 'form-control', placeholder: "Email адрес" %>
      </div>

      <div class="form-group">
        <%= f.text_field :full_name, autofocus: false, class: 'form-control', placeholder: "ФИО" %>
      </div>

      <div class="form-group">
        <%= f.text_field :faculty, autofocus: false, class: 'form-control', placeholder: "Факультет" %>
      </div>

      <div class="form-group">
        <%= f.text_field :caf, autofocus: false, class: 'form-control', placeholder: "Кафедра" %>
      </div>

      <div class="form-group">
        <%= f.text_field :position, autofocus: false, class: 'form-control', placeholder: "Должность" %>
      </div>

      <div class="form-group">
        <%= f.text_field :tel_number, autofocus: false, class: 'form-control', placeholder: "Номер телефона" %>
      </div>

      <div class="form-group">
        <%= f.password_field :password, autocomplete: "off", class: 'form-control', placeholder: 'Пароль' %>
      </div>

      <div class="form-group">
        <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control', placeholder: 'Подтвердите пароль' %>
      </div>

      <div class="form-group">
        <%= f.submit "Зарегестрироваться", class: "btn btn-primary btn-block btn-lg" %>
      </div>
    <% end %>

    <div class="text-center">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
